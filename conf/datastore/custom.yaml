name: custom
type: CUSTOM
description: Custom file-based datastore
storage_dir: ${paths.index_storage}/custom
serialization_format: json  # json, pickle, msgpack
compression: gzip  # none, gzip, lz4
chunk_size: 10000